# python-packeges-notes

python3 -m venv .venv
source .venv/bin/activate

pip3 install tqdm

pip3 install pip --upgrade
pip3 install -e .
After that you can

python3 -m shawn_r // Run through the main function.

pip3 install build
python3 -m build

pip3 install twine
python3 -m twine upload dist/* --skip